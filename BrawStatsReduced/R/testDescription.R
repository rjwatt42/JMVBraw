# 
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("II"))))
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("IO"))))
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("IC"))))
# 
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("OI"))))
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("OO"))))
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("OC"))))
# 
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("CI"))))
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("CO"))))
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("CC"))))
# 
# 
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("III"))))
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("IIC"))))
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("ICI"))))
# showDescription(makeAnalysis(makeSample(hypothesis=getHypothesis("ICC"))))
