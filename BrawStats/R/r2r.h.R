
# This file is automatically generated, you probably don't want to edit this

r2rOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "r2rOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            rvalue = 0,
            DVsd = 0, ...) {

            super$initialize(
                package="BrawStats",
                name="r2r",
                requiresData=FALSE,
                ...)

            private$..rvalue <- jmvcore::OptionNumber$new(
                "rvalue",
                rvalue,
                default=0)
            private$..DVsd <- jmvcore::OptionNumber$new(
                "DVsd",
                DVsd,
                default=0)

            self$.addOption(private$..rvalue)
            self$.addOption(private$..DVsd)
        }),
    active = list(
        rvalue = function() private$..rvalue$value,
        DVsd = function() private$..DVsd$value),
    private = list(
        ..rvalue = NA,
        ..DVsd = NA)
)

r2rResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "r2rResults",
    inherit = jmvcore::Group,
    active = list(
        sd_text = function() private$.items[["sd_text"]],
        d_text = function() private$.items[["d_text"]],
        f_text = function() private$.items[["f_text"]],
        r_text = function() private$.items[["r_text"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="correlation to r")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="sd_text",
                title=" "))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="d_text",
                title=" "))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="f_text",
                title=" "))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="r_text",
                title=" "))}))

r2rBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "r2rBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "BrawStats",
                name = "r2r",
                version = c(1,0,0),
                options = options,
                results = r2rResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'na')
        }))

#' correlation to r
#'
#' 
#' @param rvalue .
#' @param DVsd .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$sd_text} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$d_text} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$f_text} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$r_text} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
r2r <- function(
    rvalue = 0,
    DVsd = 0) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("r2r requires jmvcore to be installed (restart may be required)")


    options <- r2rOptions$new(
        rvalue = rvalue,
        DVsd = DVsd)

    analysis <- r2rClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

